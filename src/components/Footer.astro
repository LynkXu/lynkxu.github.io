---
const today = new Date();
const currentYear = today.getFullYear();
---

<footer>
	<p class='copyright'>
		© 2020 – {currentYear} <a href='/'>LynkXu</a>
	</p>
	<p class='credit'>Powered by <a href='https://astro.build' target='_blank' rel='noopener noreferrer'>Astro</a>, Design by Lynk</p>
</footer>

<style>
	footer {
		display: flex;
		align-items: center;
		justify-content: space-between;
		flex-wrap: wrap;
		gap: 0.5rem;
		padding: clamp(1.7rem, 3.4vw, 2.3rem) 0 clamp(1rem, 2vw, 1.4rem);
		width: 100%;
		border-top: 1px solid color-mix(in oklab, var(--paper-card-border-soft) 50%, transparent);
	}

	/* Fix for full-width layout pages */
	:global(body.full-width) footer {
		max-width: 1200px;
		margin: 0 auto;
		padding: var(--space-lg) var(--page-inline);
		box-sizing: border-box;
	}

	.copyright,
	.credit {
		margin: 0;
		color: var(--gray-color);
		font-family: var(--ui-font);
		font-size: 0.72rem;
		font-weight: 300;
		line-height: 1.5;
		opacity: 0.48;
		letter-spacing: 0.01em;
		transition: opacity 0.2s ease;
	}

	.copyright a,
	.credit a {
		color: inherit;
		text-decoration: none;
		border-bottom: 1px solid transparent;
		transition: border-color 0.2s, color 0.2s;
	}

	.copyright a:hover,
	.credit a:hover {
		border-bottom-color: color-mix(in oklab, var(--gray-color) 40%, transparent);
	}

	footer:hover .copyright,
	footer:hover .credit {
		opacity: 0.65;
	}

	@media (max-width: 480px) {
		footer {
			flex-direction: column;
			align-items: flex-start;
			gap: 0.4rem;
		}
	}
</style>
