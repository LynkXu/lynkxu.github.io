---
import type { CollectionEntry } from 'astro:content';
import { getCollection } from 'astro:content';
import Base from './Base.astro';
import { marked } from 'marked';
import { INCLUDE_DRAFTS, filterDrafts } from '../utils/drafts';
import fs from 'fs';
import path from 'path';
import LikeButton from '../components/LikeButton.astro';

type Props = CollectionEntry<'page'>['data'] & {
	content?: string;
};

const { title } = Astro.props;

// 读取 Markdown 文件中的碎碎念
const fileGossips = filterDrafts(await getCollection('gossips'));

// 读取 Mastodon 数据
let mastodonGossips: any[] = [];
try {
	const mastodonDataPath = path.join(process.cwd(), 'src/data/mastodon.json');
	if (fs.existsSync(mastodonDataPath)) {
		const mastodonData = JSON.parse(fs.readFileSync(mastodonDataPath, 'utf-8'));
		mastodonGossips = mastodonData.map((status: any) => ({
			data: {
				pubDate: new Date(status.created_at),
				slug: `mastodon-${status.id}`,
				draft: false,
				tag: 'mastodon',
			},
			body: status.content_markdown,
			_raw: status, // 保留原始数据用于扩展
		}));
	}
} catch (error) {
	console.warn('Failed to load Mastodon data:', error);
}

// 合并两个数据源并按日期排序
const gossips = [...fileGossips, ...mastodonGossips].sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const pageSize = 10;
const totalPages = gossips.length > 0 ? Math.ceil(gossips.length / pageSize) : 0;

marked.setOptions({
	breaks: true,
	gfm: true,
});

// 解析日期，返回终端风格日期字段
function getDateParts(date: Date) {
    const options: Intl.DateTimeFormatOptions = { timeZone: 'Asia/Shanghai' };
    const day = new Intl.DateTimeFormat('en-US', { ...options, day: '2-digit' }).format(date);
    const monthNum = new Intl.DateTimeFormat('en-US', { ...options, month: '2-digit' }).format(date);
    const year = new Intl.DateTimeFormat('en-US', { ...options, year: 'numeric' }).format(date);
    const time = new Intl.DateTimeFormat('en-US', { ...options, hour: '2-digit', minute: '2-digit', hour12: false }).format(date);
    return { day, monthNum, year, time };
}

function getEntryTags(tag: unknown) {
    if (Array.isArray(tag)) {
        return tag
            .filter(Boolean)
            .map(String)
            .filter((item) => item.toLowerCase() !== 'mastodon');
    }

    if (typeof tag === 'string') {
        return tag
            .split(/[,，\s]+/)
            .map((item) => item.trim())
            .filter((item) => item && item.toLowerCase() !== 'mastodon');
    }

    return [];
}
---

<link rel="stylesheet" href="/lightgallery/css/lightgallery.min.css" slot="head" />
<style is:global slot="head">
    .media-grid {
        display: grid;
        gap: 4px;
        margin-top: 8px;
        width: min(100%, 430px);
    }
    .media-item {
        display: block;
        border: 1px solid color-mix(in oklab, var(--gray-color) 14%, transparent);
        overflow: hidden;
        background: var(--code-background-color);
        cursor: zoom-in;
    }
    .media-item img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
    }

    .grid-1 { grid-template-columns: 1fr; }
    .grid-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .grid-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .grid-4 { grid-template-columns: repeat(2, minmax(0, 1fr)); }

    .grid-1 .media-item {
        aspect-ratio: 16 / 10;
        max-height: 220px;
    }

    .grid-2 .media-item,
    .grid-3 .media-item,
    .grid-4 .media-item {
        aspect-ratio: 4 / 3;
        max-height: 140px;
    }

    @media (max-width: 600px) {
        .media-grid {
            gap: 3px;
            width: 100%;
        }
        .grid-3 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
        .grid-1 .media-item {
            max-height: 190px;
        }
        .grid-2 .media-item,
        .grid-3 .media-item,
        .grid-4 .media-item {
            max-height: 120px;
        }
    }
</style>

<Base title={title} description={title} className="memos-layout blog-surface-layout">
	<div class='gossip-page'>
        <section class="gossip-hero" aria-labelledby="gossip-title">
            <h1 id="gossip-title">{title}</h1>
            <p>{gossips.length} memos, sorted by time.</p>
        </section>

		<div id='shuoshuo' class="feed-container" role="list">
            {
                gossips.length === 0 ? (
                    <div class='empty-state'>No short signals yet.</div>
                ) : (
                    gossips.map((gossip: any, index) => {
                        const { day, monthNum, year, time } = getDateParts(gossip.data.pubDate);
                        const dateStamp = `${year}-${monthNum}-${day}`;
                        const entryId = String(gossip.data.slug || `memo-${index}`);
                        const entryTags = getEntryTags(gossip.data.tag);
                        const attachments = gossip._raw?.media_attachments || [];
                        return (
                            <article class='feed-item' id={entryId} data-index={index} role="listitem">
                                <div class="entry-meta" aria-label="Memo metadata">
                                    <div class="entry-meta-main">
                                        <a class="entry-date" href={`#${entryId}`} aria-label={`Permalink for memo on ${dateStamp}`}>
                                            <time datetime={gossip.data.pubDate.toISOString()}>{dateStamp}</time>
                                        </a>
                                        <span class='card-time'>{time}</span>
                                        {entryTags.map((tag) => <span class="entry-tag">#{tag}</span>)}
                                        {INCLUDE_DRAFTS && gossip.data.draft && <span class="draft-indicator">draft</span>}
                                    </div>
                                    <div class="entry-meta-actions">
                                        <LikeButton id={entryId} type="gossip" />
                                    </div>
                                </div>
                                <div class='feed-card'>
                                    <div class='card-body markdown-body' set:html={marked.parse(gossip.body || '')} />
                                    
                                    {attachments.length > 0 && (
                                        <div class={`media-grid grid-${Math.min(attachments.length, 4)}`}>
                                            {attachments.slice(0, 4).map((media: any) => (
                                                <a href={media.url} class="media-item" data-src={media.url}  data-sub-html={media.description}>
                                                    <img src={media.preview_url || media.url} alt={media.description || ""} loading="lazy" />
                                                </a>
                                            ))}
                                        </div>
                                    )}
                                </div>
                            </article>
                        )
                    })
                )
            }
		</div>
        
		{totalPages > 1 && (
			<nav class='pagination' id='shuoshuo-pagination' aria-label='Pagination'>
                <div class='pagination-pages'>
					{Array.from({ length: totalPages }, (_, index) => {
						const pageNumber = index + 1;
						return (
							<a class='page-link' href={`?page=${pageNumber}`} data-page={pageNumber}>
								{pageNumber}
							</a>
						);
					})}
				</div>
			</nav>
		)}
	</div>

    <script is:inline src="/lightgallery/js/lightgallery.min.js"></script>
    <script is:inline>
        // Init LightGallery
        function initGallery() {
            const grids = document.querySelectorAll('.media-grid');
            grids.forEach(grid => {
                if (window.lightGallery) {
                    window.lightGallery(grid, {
                        selector: '.media-item',
                        speed: 500,
                        download: false
                    });
                }
            });
        }
        
        // Init immediately if ready
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', initGallery);
        } else {
            initGallery();
        }
    </script>
    
	<script is:inline define:vars={{ pageSize, totalPages }}>
		(() => {
			const items = Array.from(document.querySelectorAll('#shuoshuo .feed-item'));
			const pagination = document.getElementById('shuoshuo-pagination');
			const pageLinks = Array.from(document.querySelectorAll('#shuoshuo-pagination a[data-page]'));

			if (!pagination || !items.length || totalPages <= 1) {
				if (pagination) pagination.style.display = 'none';
				return;
			}

			const params = new URLSearchParams(window.location.search);
			const hashTarget = decodeURIComponent(window.location.hash.slice(1));
			const hashIndex = hashTarget ? items.findIndex((item) => item.id === hashTarget) : -1;
			const initialPage = hashIndex >= 0
				? Math.floor(hashIndex / pageSize) + 1
				: Math.min(totalPages, Math.max(1, Number(params.get('page')) || 1));
			const clampPage = (page) => Math.min(totalPages, Math.max(1, page));

			function renderPage(page) {
				const currentPage = clampPage(page);
				const start = (currentPage - 1) * pageSize;
				const end = start + pageSize;

				items.forEach((item, index) => {
					const visible = index >= start && index < end;
					item.style.display = visible ? '' : 'none';
				});

				pageLinks.forEach((link) => {
					const linkPage = Number(link.dataset.page);
					link.classList.toggle('active', linkPage === currentPage);
				});

				params.set('page', String(currentPage));
				const nextUrl = `${window.location.pathname}?${params.toString()}${window.location.hash}`;
				window.history.replaceState({}, '', nextUrl);
                
                if (window.scrollY > 100) {
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                }
			}

			renderPage(initialPage);

			pageLinks.forEach((link) => {
				link.addEventListener('click', (event) => {
					event.preventDefault();
					const targetPage = Number(link.dataset.page);
					if (!Number.isFinite(targetPage)) return;
					renderPage(targetPage);
				});
			});
		})();
	</script>

	<style>
        .gossip-page {
            max-width: 700px;
            margin: 0 auto;
            padding: 0 0 var(--space-lg);
        }

        .gossip-hero {
            position: relative;
            margin: 0 0 2rem;
            padding: 0.25rem 0 0;
        }

        .gossip-kicker {
            margin: 0 0 0.55rem;
            color: var(--gray-color);
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
            font-size: 0.72rem;
            letter-spacing: 0.13em;
            text-transform: uppercase;
        }

        .gossip-hero h1 {
            margin: 0 0 0.75rem;
            color: var(--heading-color);
            font-size: clamp(1.8rem, 5vw, 2.85rem);
            font-weight: 500;
            line-height: 1.16;
            letter-spacing: -0.055em;
        }

        .gossip-hero p:not(.gossip-kicker) {
            max-width: 38rem;
            margin: 0;
            color: var(--gray-color);
            line-height: 1.6;
        }

        .feed-container {
            display: grid;
            gap: 0.55rem;
        }

        .feed-item {
            display: block;
            padding: 0.9rem 0.95rem 0.95rem;
            border: 0;
            background: transparent;
            box-shadow: 0 1px 0 color-mix(in oklab, var(--rule-color) 18%, transparent);
            position: relative;
            scroll-margin-top: var(--space-xl);
            transition: box-shadow 0.18s ease, transform 0.18s ease, background 0.18s ease;
        }

        .feed-item::before {
            content: "";
            position: absolute;
            left: 0.95rem;
            right: 0.95rem;
            bottom: 0;
            height: 1px;
            background: linear-gradient(
                90deg,
                transparent,
                color-mix(in oklab, var(--rule-color) 26%, transparent) 18%,
                color-mix(in oklab, var(--rule-color) 20%, transparent) 82%,
                transparent
            );
            opacity: 0;
            pointer-events: none;
        }

        .feed-item:hover,
        .feed-item:focus-within {
            background: color-mix(in oklab, var(--content-bg) 82%, var(--page-bg) 18%);
            box-shadow:
                0 1px 0 color-mix(in oklab, var(--rule-color) 20%, transparent),
                0 8px 18px color-mix(in oklab, var(--heading-color) 4%, transparent);
            transform: translateY(-1px);
        }

        .feed-item:hover::before,
        .feed-item:focus-within::before {
            opacity: 1;
        }

        .feed-card {
            padding: 0;
            margin: 0;
            border: 0;
            border-radius: 0;
            background: transparent;
        }

        .entry-meta {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            gap: 0.75rem;
            padding-top: 0.12rem;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
            font-size: 0.7rem;
            line-height: 1.35;
            color: var(--gray-color);
            letter-spacing: 0.015em;
            font-variant-numeric: tabular-nums;
            margin-bottom: 0.45rem;
        }

        .entry-meta-main {
            min-width: 0;
            display: flex;
            align-items: baseline;
            flex-wrap: wrap;
            gap: 0.32rem 0.45rem;
        }

        .entry-meta-actions {
            flex: none;
            margin-left: auto;
            opacity: 0.5;
            transition: opacity 0.2s ease;
        }

        .feed-item:hover .entry-meta-actions,
        .feed-item:focus-within .entry-meta-actions {
            opacity: 0.9;
        }

        .entry-meta-actions :global(.like-btn.gossip) {
            font-size: 0.68rem;
            opacity: 1;
        }

        .entry-date {
            color: var(--heading-color);
            font-weight: 500;
            letter-spacing: 0.025em;
            border-bottom: 0;
        }

        .entry-date:hover {
            color: var(--link-color);
            border-bottom: 0;
        }

        .card-time {
            opacity: 0.86;
        }

        .entry-tag {
            color: color-mix(in oklab, var(--accent-color) 58%, var(--gray-color));
            letter-spacing: 0.02em;
        }

        .draft-indicator {
            color: color-mix(in oklab, var(--color-warning) 74%, var(--text-color));
            font-size: 0.62rem;
            letter-spacing: 0.04em;
            text-transform: lowercase;
            margin-left: 7px;
        }

        .card-body {
            color: var(--text-color);
            font-size: 0.92rem;
            line-height: 1.68;
            letter-spacing: 0;
        }

        .card-body :global(p) {
            margin: 0.2rem 0;
        }

        .card-body :global(p:first-child) {
            margin-top: 0;
        }

        .card-body :global(p:last-child) {
            margin-bottom: 0;
        }

        .card-body :global(strong) {
            color: var(--heading-color);
            font-weight: 600;
        }

        .card-body :global(a) {
            text-underline-offset: 0.14em;
            text-decoration-thickness: 0.05em;
        }

        .card-body :global(img) {
            max-width: 100%;
            margin-top: 6px;
            opacity: 0.92;
        }

        .card-body :global(blockquote) {
            margin: 0.12rem 0;
            padding-left: 0.36rem;
            border-left: 1px solid color-mix(in oklab, var(--gray-color) 38%, transparent);
            color: var(--gray-color);
            font-size: 0.78rem;
            line-height: 1.36;
        }

		.pagination {
			display: flex;
			justify-content: center;
			margin-top: var(--space-sm);
		}

		.page-link {
			display: inline-flex;
			align-items: center;
			justify-content: center;
			min-width: 30px;
            height: 30px;
			padding: 0 5px;
			margin: 0 1px;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
            font-size: 0.72rem;
			color: var(--gray-color);
			text-decoration: none;
            border: 1px solid color-mix(in oklab, var(--gray-color) 18%, transparent);
            background: color-mix(in oklab, var(--content-bg) 78%, transparent);
		}

		.page-link:hover {
            color: var(--heading-color);
            border-color: color-mix(in oklab, var(--gray-color) 26%, transparent);
            background: color-mix(in oklab, var(--content-bg) 84%, transparent);
		}

		.page-link.active {
            color: var(--heading-color);
            border-color: color-mix(in oklab, var(--gray-color) 26%, transparent);
            background: transparent;
            position: relative;
        }

        .page-link.active::before {
            content: "[";
            margin-right: 1px;
        }

        .page-link.active::after {
            content: "]";
            margin-left: 1px;
		}

        .page-link:focus-visible {
            outline: 2px solid var(--accent-color);
            outline-offset: 1px;
        }

        @media (max-width: 720px) {
            .gossip-page {
                padding-bottom: var(--space-md);
            }

            .gossip-hero {
                margin-bottom: 1.5rem;
                padding-bottom: 0.5rem;
            }

            .feed-item {
                padding: var(--space-sm);
            }

            .entry-meta {
                align-items: flex-start;
                gap: 0.5rem;
            }

            .feed-card {
                padding: 0;
            }

            .card-body {
                font-size: 0.86rem;
                line-height: 1.58;
            }

            .entry-meta {
                padding-top: 0;
                font-size: 0.64rem;
            }

        }
	</style>
</Base>
