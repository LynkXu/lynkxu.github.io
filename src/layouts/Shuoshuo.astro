---
import type { CollectionEntry } from 'astro:content';
import { getCollection } from 'astro:content';
import MagazineShell from './MagazineShell.astro';
import { marked } from 'marked';
import { INCLUDE_DRAFTS, filterDrafts } from '../utils/drafts';
import fs from 'fs';
import path from 'path';
import LikeButton from '../components/LikeButton.astro';

type Props = CollectionEntry<'page'>['data'] & {
	content?: string;
};

const { title } = Astro.props;

const fileGossips = filterDrafts(await getCollection('gossips'));

let mastodonGossips: any[] = [];
try {
	const mastodonDataPath = path.join(process.cwd(), 'src/data/mastodon.json');
	if (fs.existsSync(mastodonDataPath)) {
		const mastodonData = JSON.parse(fs.readFileSync(mastodonDataPath, 'utf-8'));
		mastodonGossips = mastodonData.map((status: any) => ({
			data: {
				pubDate: new Date(status.created_at),
				slug: `mastodon-${status.id}`,
				draft: false,
				tag: 'mastodon',
			},
			body: status.content_markdown,
			_raw: status,
		}));
	}
} catch (error) {
	console.warn('Failed to load Mastodon data:', error);
}

const gossips = [...fileGossips, ...mastodonGossips].sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const pageSize = 10;
const totalPages = gossips.length > 0 ? Math.ceil(gossips.length / pageSize) : 0;

marked.setOptions({
	breaks: true,
	gfm: true,
});

function getDateParts(date: Date) {
	const options: Intl.DateTimeFormatOptions = { timeZone: 'Asia/Shanghai' };
	const day = new Intl.DateTimeFormat('en-US', { ...options, day: '2-digit' }).format(date);
	const monthNum = new Intl.DateTimeFormat('en-US', { ...options, month: '2-digit' }).format(date);
	const year = new Intl.DateTimeFormat('en-US', { ...options, year: 'numeric' }).format(date);
	const time = new Intl.DateTimeFormat('en-US', { ...options, hour: '2-digit', minute: '2-digit', hour12: false }).format(date);
	return { day, monthNum, year, time };
}

const memoItems = gossips.map((gossip: any, index) => {
	const { day, monthNum, year, time } = getDateParts(gossip.data.pubDate);
	return {
		gossip,
		index,
		page: Math.floor(index / pageSize) + 1,
		dateLabel: `${year}.${monthNum}.${day}`,
		monthLabel: `${year}.${monthNum}`,
		year,
		time,
		attachments: gossip._raw?.media_attachments || [],
	};
});

function formatMemoDate(date?: Date) {
	if (!date) return '—';
	return new Intl.DateTimeFormat('zh-CN', {
		timeZone: 'Asia/Shanghai',
		year: 'numeric',
		month: '2-digit',
		day: '2-digit',
	}).format(date).replace(/\//g, '.');
}

const currentYear = new Intl.DateTimeFormat('en-US', { timeZone: 'Asia/Shanghai', year: 'numeric' }).format(new Date());
const activeMonthCount = new Set(memoItems.map((item) => item.monthLabel)).size;
const mediaMemoCount = memoItems.filter((item) => item.attachments.length > 0).length;
const thisYearMemoCount = memoItems.filter((item) => item.year === currentYear).length;
const latestMemo = memoItems[0];
const earliestMemo = memoItems.length > 0 ? memoItems[memoItems.length - 1] : undefined;
const memoStats = [
	{ label: 'Memos', value: gossips.length },
	{ label: 'This year', value: thisYearMemoCount },
	{ label: 'Months', value: activeMonthCount },
	{ label: 'Media', value: mediaMemoCount },
];
const sourceStats = [
	{ label: 'Local', value: fileGossips.length },
	{ label: 'Mastodon', value: mastodonGossips.length },
];
const monthStats = Array.from(
	memoItems.reduce((map, item) => {
		const entry = map.get(item.monthLabel) || { label: item.monthLabel, count: 0, media: 0 };
		entry.count += 1;
		if (item.attachments.length > 0) entry.media += 1;
		map.set(item.monthLabel, entry);
		return map;
	}, new Map<string, { label: string; count: number; media: number }>())
	.values()
).slice(0, 4);
const maxMonthCount = Math.max(1, ...monthStats.map((item) => item.count));
const latestMemoLabel = formatMemoDate(latestMemo?.gossip.data.pubDate);
const earliestMemoYear = earliestMemo?.year || '—';

const pagedMemoPages = Array.from({ length: totalPages }, (_, pageIndex) => {
	const page = pageIndex + 1;
	return {
		page,
		items: memoItems.filter((item) => item.page === page),
	};
});

---

<link rel="stylesheet" href="/lightgallery/css/lightgallery.min.css" slot="head" />
<style is:global slot="head">
    .media-grid {
        display: grid;
        gap: 4px;
        margin-top: 8px;
        width: min(100%, 430px);
    }
    .media-item {
        display: block;
        border: 1px solid color-mix(in oklab, var(--gray-color) 14%, transparent);
        overflow: hidden;
        background: var(--code-background-color);
        cursor: zoom-in;
    }
    .media-item img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
    }

    .grid-1 { grid-template-columns: 1fr; }
    .grid-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .grid-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .grid-4 { grid-template-columns: repeat(2, minmax(0, 1fr)); }

    .grid-1 .media-item {
        aspect-ratio: 16 / 10;
        max-height: 220px;
    }

    .grid-2 .media-item,
    .grid-3 .media-item,
    .grid-4 .media-item {
        aspect-ratio: 4 / 3;
        max-height: 140px;
    }

    @media (max-width: 600px) {
        .media-grid {
            gap: 3px;
            width: 100%;
        }
        .grid-3 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
        .grid-1 .media-item {
            max-height: 190px;
        }
        .grid-2 .media-item,
        .grid-3 .media-item,
        .grid-4 .media-item {
            max-height: 120px;
        }
    }
</style>

<MagazineShell title={title} description={title} className="memos-surface" showProfileCard={false}>
	<section class="memos-panel magazine-panel" aria-labelledby="memos-title">
		<header class="memos-panel__header">
			<p class="magazine-section-label">Timeline</p>
			<h1 id="memos-title" class="sr-only">Memos</h1>
			<p>Short notes gathered by time. Filters stay secondary.</p>
		</header>

		<div class="memos-layout">
			<div class="memos-feed-column">
			<div id='shuoshuo' class="memos-feed">
				{
					gossips.length === 0 ? (
						<div class='empty-state'>No memos yet.</div>
					) : (
						pagedMemoPages.map(({ page, items }) => (
							<div class="memos-page" data-page={page}>
								<div class="memos-page-list" role="list">
									{items.map((item) => {
										const { gossip, attachments, dateLabel, time, index } = item;
										return (
											<article class='memo-item' data-index={index} role="listitem">
												<a href="/" class="memo-avatar-link" aria-label="LynkXu">
													<img src="/ava4.png" alt="" class="memo-avatar" loading={index === 0 ? 'eager' : 'lazy'} width="40" height="40" />
												</a>
												<div class='memo-body'>
													<header class="memo-header">
														<div class="memo-header-top">
															<div class="memo-author-line">
																<span class="memo-author">LynkXu</span>
																{gossip.data.tag && <span class="memo-source">{gossip.data.tag}</span>}
															</div>
															<div class="memo-actions">
																{INCLUDE_DRAFTS && gossip.data.draft && <span class="draft-indicator">draft</span>}
																<LikeButton id={gossip.data.slug} type="gossip" />
															</div>
														</div>
														<time class="memo-meta-time" datetime={gossip.data.pubDate.toISOString()}>
															{dateLabel} · {time}
														</time>
													</header>

													<div class='memo-content markdown-body' set:html={marked.parse(gossip.body || '')} />

													{attachments.length > 0 && (
														<div class={`media-grid grid-${Math.min(attachments.length, 4)}`}>
															{attachments.slice(0, 4).map((media: any) => (
																<a href={media.url} class="media-item" data-src={media.url}  data-sub-html={media.description}>
																	<img src={media.preview_url || media.url} alt={media.description || ""} loading="lazy" />
																</a>
															))}
														</div>
													)}
												</div>
											</article>
										)
									})}
								</div>
							</div>
						))
					)
				}
			</div>

			{totalPages > 1 && (
				<nav class='pagination' id='shuoshuo-pagination' aria-label='Pagination'>
					<div class='pagination-pages'>
						{Array.from({ length: totalPages }, (_, index) => {
							const pageNumber = index + 1;
							return (
								<a class='page-link' href={`?page=${pageNumber}`} data-page={pageNumber}>
									{pageNumber}
								</a>
							);
						})}
					</div>
				</nav>
			)}
		</div>
	</div>
	</section>

	<div slot="sidebar-extra" class="memos-rail">
		<section class="magazine-rail-card memos-filter-card" aria-labelledby="memos-filter-title">
			<p id="memos-filter-title" class="magazine-section-label">Filters</p>
			<div class="memos-filter-chips" aria-label="Visual filters">
				<span class="active">All</span>
				<span>Local</span>
				<span>Mastodon</span>
				<span>Media</span>
			</div>
		</section>

		<section class="magazine-rail-card memos-month-card" aria-labelledby="memos-month-title">
			<p id="memos-month-title" class="magazine-section-label">Months</p>
			<div class="memos-month-list">
				{monthStats.map((month) => (
					<div class="memos-month-row">
						<strong>{month.label}</strong>
						<span class="memos-month-meter" style={`--month-size: ${(month.count / maxMonthCount) * 100}%`} aria-hidden="true"></span>
						<em>{month.count}</em>
					</div>
				))}
			</div>
			<p>{gossips.length} memos · {mediaMemoCount} media</p>
		</section>

		<section class="magazine-rail-card memos-source-card" aria-labelledby="memos-source-title">
			<p id="memos-source-title" class="magazine-section-label">Sources</p>
			<dl class="memos-source-list">
				{sourceStats.map((item) => (
					<div class="memos-source-row">
						<dt>{item.label === 'Local' ? 'Local markdown' : 'Remote statuses'}</dt>
						<dd>{item.value}</dd>
					</div>
				))}
			</dl>
		</section>
	</div>
</MagazineShell>

<script is:inline src="/lightgallery/js/lightgallery.min.js" defer></script>
<script is:inline>
    function initGallery() {
        if (!window.lightGallery) return;
        const grids = document.querySelectorAll('.media-grid');
        grids.forEach(grid => {
            window.lightGallery(grid, {
                selector: '.media-item',
                speed: 500,
                download: false
            });
        });
    }

    function onReady() {
        if (window.lightGallery) {
            initGallery();
        } else {
            setTimeout(onReady, 100);
        }
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', onReady);
    } else {
        onReady();
    }
</script>

<script is:inline define:vars={{ totalPages }}>
	(() => {
		const pages = Array.from(document.querySelectorAll('#shuoshuo .memos-page'));
		const pagination = document.getElementById('shuoshuo-pagination');
		const pageLinks = Array.from(document.querySelectorAll('#shuoshuo-pagination a[data-page]'));

		if (!pagination || !pages.length || totalPages <= 1) {
			if (pagination) pagination.style.display = 'none';
			return;
		}

		const params = new URLSearchParams(window.location.search);
		const initialPage = Math.min(totalPages, Math.max(1, Number(params.get('page')) || 1));
		const clampPage = (page) => Math.min(totalPages, Math.max(1, page));

		function renderPage(page) {
			const currentPage = clampPage(page);

			pages.forEach((pageNode) => {
				const visible = Number(pageNode.dataset.page) === currentPage;
				pageNode.hidden = !visible;
			});

			pageLinks.forEach((link) => {
				const linkPage = Number(link.dataset.page);
				const active = linkPage === currentPage;
				link.classList.toggle('active', active);
				if (active) {
					link.setAttribute('aria-current', 'page');
				} else {
					link.removeAttribute('aria-current');
				}
			});

			params.set('page', String(currentPage));
			const nextUrl = `${window.location.pathname}?${params.toString()}${window.location.hash}`;
			window.history.replaceState({}, '', nextUrl);
            
            if (window.scrollY > 100) {
                window.scrollTo({ top: 0, behavior: 'smooth' });
            }
		}

		renderPage(initialPage);

		pageLinks.forEach((link) => {
			link.addEventListener('click', (event) => {
				event.preventDefault();
				const targetPage = Number(link.dataset.page);
				if (!Number.isFinite(targetPage)) return;
				renderPage(targetPage);
			});
		});
	})();
</script>

<style>
	:global(.memos-surface) {
		--blog-shell-width: 1125px;
		--blog-shell-sidebar-width: 18rem;
		--blog-shell-gap: var(--memos-column-gap);
		--memos-feed-width: 100%;
		--memos-column-gap: clamp(var(--space-lg), 2.2vw, var(--space-xl));
		--memos-section-gap: 0.72rem;
	}

	.sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	.memos-panel {
		min-height: min(52rem, calc(100vh - 10rem));
	}

	.memos-panel__header {
		margin-bottom: clamp(var(--space-lg), 3vw, var(--space-xl));
	}

	.memos-panel__header p:last-child {
		margin: 0.72rem 0 0;
		color: var(--gray-color);
		font-family: var(--ui-font);
		font-size: 0.88rem;
		font-weight: var(--weight-semibold);
		line-height: 1.5;
	}

	.memos-layout {
		display: block;
	}

	.memos-feed-column {
		grid-area: feed;
		display: flex;
		flex-direction: column;
		gap: var(--memos-section-gap);
		min-width: 0;
	}

	.memos-rail {
		display: flex;
		flex-direction: column;
		gap: clamp(var(--space-md), 2vw, var(--space-lg));
	}

	.memos-stats-kicker {
		margin: 0 0 0.62rem;
		font-family: var(--field-font);
		font-size: 0.68rem;
		font-weight: var(--weight-medium);
		line-height: 1.35;
		color: color-mix(in oklab, var(--heading-color) 80%, var(--gray-color));
		letter-spacing: 0.04em;
	}

	.memos-stats-list {
		display: flex;
		flex-direction: column;
		gap: 0;
		margin: 0;
	}

	.memos-stat-row {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		gap: 0.75rem;
		padding: 0.42rem 0;
		border-top: 1px solid var(--paper-card-border-soft);
	}

	.memos-stat-row dt {
		font-size: 0.66rem;
		line-height: 1.35;
		color: var(--gray-color);
		letter-spacing: 0.01em;
	}

	.memos-stat-row dd {
		margin: 0;
		font-family: var(--field-font);
		font-size: 0.8rem;
		font-weight: var(--weight-medium);
		line-height: 1.25;
		color: var(--heading-color);
		font-variant-numeric: tabular-nums;
	}

	.memos-source-list {
		display: flex;
		flex-direction: column;
		gap: 0.38rem;
		margin: 0;
	}

	.memos-source-row {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		gap: 0.7rem;
		font-size: 0.64rem;
		line-height: 1.4;
	}

	.memos-source-row dt {
		color: var(--gray-color);
	}

	.memos-source-row dd {
		margin: 0;
		font-family: var(--field-font);
		color: color-mix(in oklab, var(--heading-color) 84%, var(--gray-color));
		font-size: 0.72rem;
		font-weight: var(--weight-medium);
		font-variant-numeric: tabular-nums;
	}

	.memos-stats-foot {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		row-gap: 0.18rem;
		column-gap: 0.6rem;
		margin-top: 0.54rem;
		padding-top: 0.58rem;
		border-top: 1px solid var(--paper-card-border-soft);
		font-size: 0.62rem;
		line-height: 1.4;
		color: var(--faint-color);
	}

	.memos-stats-foot time,
	.memos-stats-foot span:nth-child(2n) {
		font-family: var(--field-font);
		color: color-mix(in oklab, var(--gray-color) 86%, var(--heading-color));
		font-variant-numeric: tabular-nums;
	}

	.memos-feed {
		display: flex;
		flex-direction: column;
		gap: var(--memos-section-gap);
		margin-top: 0 !important;
	}

	.memos-page {
		display: flex;
		flex-direction: column;
		gap: var(--memos-section-gap);
	}

	.memos-page[hidden] {
		display: none;
	}

	.memos-page-list {
		display: flex;
		flex-direction: column;
		gap: var(--memos-section-gap);
	}

	.memo-item {
		display: grid;
		grid-template-columns: 2.5rem minmax(0, 1fr);
		gap: 0.8rem;
		padding: clamp(0.9rem, 2vw, 1.12rem);
		position: relative;
		border: 1px solid var(--paper-card-border-soft);
		border-radius: var(--paper-card-radius);
		background: var(--paper-card-bg);
		box-shadow: var(--paper-card-shadow);
		transition: background 0.2s ease, border-color 0.2s ease;
	}

	.memo-item:hover {
		background: var(--paper-hover-bg);
		border-color: var(--paper-hover-border);
	}

	.memo-avatar-link {
		display: block;
		border: none !important;
		background: none !important;
		line-height: 0;
		border-radius: 50%;
	}

	.memo-avatar-link:hover {
		transform: none;
	}

	.memo-avatar {
		width: 40px;
		height: 40px;
		border-radius: 50%;
		object-fit: cover;
		border: 0;
		background: color-mix(in oklab, var(--accent-soft-bg) 72%, var(--paper-field-bg));
	}

	.memo-body {
		display: flex;
		flex-direction: column;
		min-width: 0;
	}

	.memo-header {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 0.12rem;
		margin-bottom: 0.16rem;
		line-height: 1.35;
	}

	.memo-header-top {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 0.65rem;
		width: 100%;
		min-width: 0;
	}

	.memo-author-line {
		display: flex;
		align-items: center;
		flex-wrap: wrap;
		gap: 0.4rem;
		min-width: 0;
	}

	.memo-author {
		color: var(--heading-color);
		font-family: var(--ui-font);
		font-size: 0.92rem;
		font-weight: var(--weight-bold);
	}

	.memo-actions {
		display: inline-flex;
		align-items: center;
		justify-content: flex-end;
		gap: 0.4rem;
		flex-shrink: 0;
		margin-top: -0.04rem;
		opacity: 0.74;
		transition: opacity 0.2s ease;
	}

	.memo-item:hover .memo-actions,
	.memo-item:focus-within .memo-actions {
		opacity: 1;
	}

	.memo-source {
		display: inline-flex;
		align-items: center;
		min-height: 1.05rem;
		padding: 0 0.36rem;
		border: 1px solid color-mix(in oklab, var(--paper-card-border-soft) 72%, transparent);
		border-radius: 999px;
		background: color-mix(in oklab, var(--paper-field-bg) 70%, transparent);
		font-family: var(--field-font);
		font-size: 0.58rem;
		color: var(--gray-color);
		letter-spacing: 0.02em;
	}

	.memo-meta-time {
		font-family: var(--field-font);
		font-size: 0.72rem;
		line-height: 1.35;
		color: var(--gray-color);
		font-variant-numeric: tabular-nums;
	}

	.memo-content {
		color: var(--text-color);
		font-size: 0.94rem;
		line-height: 1.68;
		letter-spacing: 0;
		margin-bottom: 0.16rem;
	}

	.memo-content :global(p) {
		margin: 0.08rem 0;
	}

	.memo-content :global(p:first-child) {
		margin-top: 0;
	}

	.memo-content :global(p:last-child) {
		margin-bottom: 0;
	}

	.memo-content :global(strong) {
		color: var(--heading-color);
		font-weight: var(--weight-semibold);
	}

	.memo-content :global(a) {
		text-underline-offset: 0.14em;
		text-decoration-thickness: 0.05em;
	}

	.memo-content :global(img) {
		max-width: 100%;
		margin-top: 6px;
		opacity: 0.92;
		border-radius: 3px;
	}

	.memo-content :global(blockquote) {
		margin: 0.28rem 0;
		padding-left: 0.62rem;
		border-left: 1px solid color-mix(in oklab, var(--gray-color) 38%, transparent);
		color: var(--gray-color);
		font-size: 0.82rem;
		line-height: 1.48;
	}

	.memo-tag,
	.draft-indicator {
		font-family: var(--field-font);
		font-size: 0.65rem;
		line-height: 1.35;
		color: var(--gray-color);
		letter-spacing: 0.035em;
	}

	.memo-tag::before {
		content: "#";
		opacity: 0.55;
	}

	.draft-indicator {
		color: color-mix(in oklab, var(--color-warning) 74%, var(--text-color));
		text-transform: lowercase;
	}

	.empty-state {
		text-align: center;
		padding: 3rem 1rem;
		color: var(--gray-color);
		font-size: 0.9rem;
	}

	.pagination {
		display: flex;
		justify-content: center;
		margin-top: var(--space-md);
	}

	.memos-filter-card,
	.memos-month-card,
	.memos-source-card {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.memos-filter-chips {
		display: flex;
		flex-wrap: wrap;
		gap: 0.82rem;
	}

	.memos-filter-chips span {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-width: 4.15rem;
		min-height: 1.75rem;
		padding: 0.25rem 0.75rem;
		border-radius: 5px;
		background: color-mix(in oklab, var(--paper-field-bg) 76%, var(--accent-soft-bg));
		color: var(--heading-color);
		font-family: var(--ui-font);
		font-size: 0.76rem;
		font-weight: var(--weight-bold);
		line-height: 1;
	}

	.memos-filter-chips span.active {
		color: color-mix(in oklab, var(--accent-color) 72%, var(--heading-color));
		background: color-mix(in oklab, var(--accent-soft-bg) 70%, var(--paper-card-bg));
	}

	.memos-month-list {
		display: flex;
		flex-direction: column;
		gap: 0.8rem;
	}

	.memos-month-row {
		display: grid;
		grid-template-columns: 5rem minmax(4rem, 1fr) auto;
		align-items: center;
		gap: 0.7rem;
		font-family: var(--ui-font);
	}

	.memos-month-row strong {
		color: var(--heading-color);
		font-size: 1.02rem;
		font-weight: var(--weight-bold);
		font-variant-numeric: tabular-nums;
		line-height: 1;
	}

	.memos-month-row em {
		color: color-mix(in oklab, var(--accent-color) 72%, var(--heading-color));
		font-size: 0.92rem;
		font-style: normal;
		font-weight: var(--weight-bold);
		font-variant-numeric: tabular-nums;
	}

	.memos-month-meter {
		position: relative;
		height: 0.5rem;
		border-radius: 999px;
		background: color-mix(in oklab, var(--accent-soft-bg) 46%, var(--paper-field-bg));
		overflow: hidden;
	}

	.memos-month-meter::before {
		content: "";
		position: absolute;
		inset: 0 auto 0 0;
		width: var(--month-size);
		border-radius: inherit;
		background: color-mix(in oklab, var(--accent-color) 82%, var(--heading-color));
	}

	.memos-month-card p {
		margin: 0;
		color: var(--gray-color);
		font-family: var(--ui-font);
		font-size: 0.9rem;
		font-weight: var(--weight-semibold);
		line-height: 1.5;
	}

	.pagination-pages {
		display: inline-flex;
		align-items: center;
		gap: 0.12rem;
		padding: 0.18rem;
		border: 1px solid var(--paper-card-border-soft);
		border-radius: var(--paper-card-radius);
		background: color-mix(in oklab, var(--paper-card-bg-soft) 72%, transparent);
	}

	.pagination .page-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-width: 1.55rem;
		height: 1.45rem;
		padding: 0 0.38rem;
		font-family: var(--field-font);
		font-size: 0.68rem;
		line-height: 1;
		color: var(--gray-color);
		text-decoration: none;
		border: 1px solid transparent;
		border-radius: calc(var(--paper-card-radius) - 1px);
		background: transparent;
		font-variant-numeric: tabular-nums;
		transition: color 0.18s ease, background 0.18s ease, border-color 0.18s ease;
	}

	.pagination .page-link:hover {
		color: color-mix(in oklab, var(--heading-color) 88%, var(--gray-color));
		background: color-mix(in oklab, var(--paper-card-bg) 72%, var(--page-bg));
	}

	.pagination .page-link.active {
		color: var(--heading-color);
		border-color: color-mix(in oklab, var(--gray-color) 18%, transparent);
		background: var(--paper-card-bg);
		position: relative;
	}

	.pagination .page-link.active::before,
	.pagination .page-link.active::after {
		content: none;
	}

	.pagination .page-link:focus-visible {
		outline: 2px solid var(--accent-color);
		outline-offset: 2px;
	}

	@keyframes fade-up {
		from { opacity: 0; transform: translateY(10px); }
		to { opacity: 1; transform: translateY(0); }
	}

	@media (max-width: 1023px) {
		:global(.memos-surface) {
			--memos-section-gap: 0.82rem;
		}

		:global(.memos-surface .magazine-shell) {
			grid-template-columns: 1fr;
			justify-content: stretch;
		}

		:global(.memos-surface .magazine-shell),
		:global(.memos-surface .magazine-main) {
			gap: var(--memos-section-gap);
		}

		.memos-page {
			gap: var(--memos-section-gap);
		}

		.memos-layout {
			width: 100%;
		}

		.memo-item {
			grid-template-columns: 2.1rem minmax(0, 1fr);
			gap: 0.56rem;
			padding: 0.7rem 0.76rem 0.62rem;
		}

		.memo-avatar {
			width: 34px;
			height: 34px;
		}

		.memo-content {
			font-size: 0.88rem;
			line-height: 1.6;
		}

		.memo-header-top {
			gap: 0.5rem;
		}

		.memo-actions {
			opacity: 0.82;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.memos-feed {
			animation: none;
		}
	}
</style>
