---
import MagazineShell from './MagazineShell.astro';

const { title } = Astro.props;
---

<MagazineShell title={title} description={title}>
	<div class="about-hero">
		<h1 class="about-hero__title">👋 I am Lynk</h1>
		<p class="about-hero__desc">Fullstack Engineer in Shanghai</p>
		<p>你好，我是 Lynk，一名在上海工作的全栈（后端）工程师。</p>
		<p>很高兴你发现了这里，也希望你喜欢我写的内容。祝你拥有美好的一天。</p>
	</div>
	<div class="about-sheet">
		<slot />
	</div>
</MagazineShell>

<style is:global>
	/* About hero */
	.about-hero {
		position: relative;
		border: 1px solid var(--paper-card-border-soft);
		border-radius: var(--paper-card-radius);
		background:
			linear-gradient(180deg, color-mix(in oklab, var(--accent-soft-bg) 36%, var(--paper-card-bg)) 0%, var(--paper-card-bg) 100%);
		box-shadow: var(--paper-card-shadow);
		padding: clamp(1.45rem, 4vw, 2.05rem);
		animation: fade-up 0.5s ease-out both;
	}

	.about-hero__title {
		margin: 10;
		font-family: var(--serif-font);
		font-size: clamp(1.32rem, 2.35vw, 1.58rem);
		font-weight: 500;
		letter-spacing: -0.014em;
		color: var(--panel-title-color);
		line-height: 1.22;
	}

	.about-hero__desc {
		margin: 0.45rem 0 0;
		font-size: 0.84rem;
		color: var(--gray-color);
		line-height: 1.55;
	}

	/* About sheet sits directly in magazine-main; let inner cards breathe */
	.magazine-main > .about-sheet {
		animation: fade-up 0.5s ease-out 0.08s both;
	}

	.magazine-main > .about-sheet > :first-child {
		animation: fade-up 0.5s ease-out 0.14s both;
	}

	.magazine-main > .about-sheet > :nth-child(2) {
		animation: fade-up 0.5s ease-out 0.2s both;
	}

	.magazine-main > .about-sheet > :nth-child(3) {
		animation: fade-up 0.5s ease-out 0.26s both;
	}

	@media (prefers-reduced-motion: reduce) {
		.about-hero,
		.magazine-main > .about-sheet,
		.magazine-main > .about-sheet > * {
			animation: none;
		}
	}
</style>
